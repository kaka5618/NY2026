{
  "name": "virtual-boyfriend-companion",
  "private": true,
  "workspaces": [
    "web",
    "shared"
  ],
  "scripts": {
    "dev": "npm run dev -w web",
    "dev:clean": "sh -c 'lsof -ti tcp:3000 | xargs kill 2>/dev/null || true; lsof -ti tcp:3001 | xargs kill 2>/dev/null || true; rm -rf web/.next; npm run dev -w web'",
    "build": "npm run build -w web",
    "start": "npm run start -w web"
  }
}
